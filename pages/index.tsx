import React from 'react';
import {SectionTemplate} from 'ui/templates/SectionTemplate';

export default () => {
    return (
        <SectionTemplate>
            <h1>{'Template'}</h1>
        </SectionTemplate>
    );
};
