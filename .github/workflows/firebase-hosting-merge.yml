# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - develop
env:
 NEXT_PUBLIC_API_KEY: '${{ secrets.FIREBASE_API_KEY }}'
 NEXT_PUBLIC_AUTH_DOMAIN: "nikita-kirdiapin-blog.firebaseapp.com"
 NEXT_PUBLIC_DATABASE_URL: "https://nikita-kirdiapin-blog-default-rtdb.europe-west1.firebasedatabase.app"
 NEXT_PUBLIC_PROJECT_ID: "nikita-kirdiapin-blog"
 NEXT_PUBLIC_STORAGE_BUCKET: "nikita-kirdiapin-blog.appspot.com"
 NEXT_PUBLIC_MESSAGING_SENDER_ID: "477241621412"
 NEXT_PUBLIC_APP_ID: "1:477241621412:web:6fc6bec00c5bcc580c9f31"
 NEXT_PUBLIC_MEASUREMENT_ID: "G-PFHYTY91VL"
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Cache NPM dependencies
        uses: actions/cache@v2
        with:
          # See here for caching with `yarn` https://github.com/actions/cache/blob/main/examples.md#node---yarn or you can leverage caching with actions/setup-node https://github.com/actions/setup-node
          path: |
            ~/.npm
            ${{ github.workspace }}/.next/cache
          # Generate a new cache whenever packages or source files change.
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}-${{ hashFiles('**.[jt]s', '**.[jt]sx') }}
          # If source files changed but packages didn't, rebuild from a prior cache.
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}-
      - name: Install NPM dependencies
        run: npm ci && npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NIKITA_KIRDIAPIN_BLOG }}'
